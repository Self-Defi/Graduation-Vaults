# Graduation-Vaults
A read-only UI + data layer for student Graduation Vaults. Includes SAFE API integrations, Cloudflare Worker proxy, and static GitHub-hosted dashboard for transparent viewing of each student vault. 
